[tasks.clean]
confirm = "Are youn sure?"
description = "Clean up"
run = "cargo clean"

[tasks.update]
description = "Update dependencies"
run = "cargo update"

[tasks.test]
description = "Run a test"
run = "cargo test"

[tasks.hello]
description = "Run the hello example"
run = "cargo run -p hello"
