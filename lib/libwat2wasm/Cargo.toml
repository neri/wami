[package]
name = "libwat2wasm"
version = "0.1.0"
edition = "2024"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[features]
# default = ["alloc"]
# alloc = []

[dependencies]
leb128 = { path = "../leb128" }
